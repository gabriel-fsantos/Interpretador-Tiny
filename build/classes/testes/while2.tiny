writeStr("Entre com o numero de linhas: ");
read(n);
if n < 0 then
   writeStr("numero de linhas menor que 0");
else    
  a:= 1;
  while  a <= n do
    writeVar(a);
	 writeln;
	 writeStr("i's:");
	 i:=1;
	 while i <= 3 do
	 	writeVar(i);
		i:= i + 1;
	 endw
    a:= a + 1;
	 writeln;
	 writeln;
  endw
endif
endp